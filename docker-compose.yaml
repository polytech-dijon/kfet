version: '3.8'

services:
  db:
    container_name: kfet-db
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - kfet-network

  app:
    container_name: kfet-app
    image: node:18-bullseye
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run db:migrate && npm run db:seed && npm run build && npm run start"
    ports:
      - "3000:3000"
    expose:
      - "3000"
    depends_on:
      - db
    networks:
      - kfet-network

volumes:
  postgres_data:

networks:
  kfet-network:
    driver: bridge
